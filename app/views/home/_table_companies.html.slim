.table-responsive

  / = simple_form_for @q, url: root_path, html: {class: 'form-vertical'} do |f|

    / Search if the name field contains...
    / = f.input :name_cont

    / = f.button :submit

  table.table.table-hover
    thead
      tr
        th= sort_link(@q, :name)
        th= sort_link(@q, :description)
        th= sort_link(@q, :long_description)
    tbody
      - @companies.each do |company|
        tr
          td= company.name
          td= company.description
          td= company.long_description
  = will_paginate @companies, renderer: BootstrapPagination::Rails
