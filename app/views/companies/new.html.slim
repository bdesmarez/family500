.container
  br
  .row
    .col-lg-12
      = simple_form_for @company, html: {class: 'form-horizontal'} do |f|
        = f.input :name
        = f.input :alias
        = f.input :family_name
        = f.input :creation_date, as: :date
        = f.input :branch
        = f.input :postal_code
        = f.input :city
        = f.input :phone
        = f.input :mobile
        = f.input :fax
        = f.input :email

        / Ranking form
        = f.simple_fields_for :rankings do |fr|
          = fr.input :year
          = fr.input :turnover
          = fr.input :employees

        / Contact form
        = f.simple_fields_for :contacts do |fc|
          = fc.input :civility
          = fc.input :title
          = fc.input :name
          = fc.input :firstname
          = fc.input :position
          = fc.input :phone
          = fc.input :mobile
          = fc.input :fax
          = fc.input :email


        = f.button :submit, class: 'btn-danger'


javascript:
  $(document).ready(function(){
    var input = $('#company_phone');
    var intl_tel = input.val();

    input.intlTelInput({
      defaultCountry: 'de',
      onlyCountries: ['be', 'fr', 'ch', 'de'],
      utilsScript: 'https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/7.1.1/lib/libphonenumber/build/utils.js',
      nationalMode: true
    });

  });
/ utilsScript: "#{javascript_path 'libphonenumber' }",
